<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Pokemon</title>
</head>
<body>
  <h4>Editing: {{pokemon.name}}</h4>
  <form class="pokemon-form" method="POST" action="/pokemons/{{pokemon.id}}?_method=PUT">
    <div class="pokemon-attribute">
      id:<input name="id" type="text" value="{{pokemon.id}}" readonly/>
    </div>
    <div class="pokemon-attribute">
      num:<input name="num" type="text" value="{{pokemon.num}}" readonly/>
    </div>
    <div class="pokemon-attribute">
      name:<input name="name" type="text" value="{{pokemon.name}}"/>
    </div>
    <div class="pokemon-attribute">
      img:<input name="img" type="text" value="{{pokemon.img}}"/>
    </div>
    <div class="pokemon-attribute">
      height:<input name="height" type="text" value="{{pokemon.height}}"/>
    </div>
    <div class="pokemon-attribute">
      weight:<input name="weight" type="text" value="{{pokemon.weight}}"/>
    </div>
    <br>
    <input name="submit" type="submit"/>
  </form>
</body>
</html>
